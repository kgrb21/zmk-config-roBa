#include <dt-bindings/zmk/matrix_transform.h>
#include <dt-bindings/input/input-event-codes.h>
#include <zephyr/dt-bindings/gpio/gpio.h>

/ {
    chosen {
        zmk,kscan = &kscan0;
        zmk,matrix-transform = &default_transform;
    };

    kscan0: kscan {
        compatible = "zmk,kscan-gpio-charlieplex";
        wakeup-source;

        /* XIAO BLE D0 = P0.02 なので直接指定します */
        interrupt-gpios = <&gpio0 2 GPIO_ACTIVE_HIGH>;

        /* 6つのピンを直接指定 (D1, D2, D3, D6, D7, D8) */
        gpios
            = <&gpio0 3 GPIO_ACTIVE_HIGH>  /* D1 */
            , <&gpio0 28 GPIO_ACTIVE_HIGH> /* D2 */
            , <&gpio0 29 GPIO_ACTIVE_HIGH> /* D3 */
            , <&gpio1 11 GPIO_ACTIVE_HIGH> /* D6 */
            , <&gpio0 11 GPIO_ACTIVE_HIGH> /* D7 */
            , <&gpio1 12 GPIO_ACTIVE_HIGH> /* D8 */
            ;
    };

    default_transform: matrix_transform {
        compatible = "zmk,matrix-transform";
        rows = <10>;
        columns = <6>;
        map = <
            RC(1,0) RC(0,1) RC(0,2) RC(0,3) RC(0,4) RC(0,5)  RC( 7,0) RC( 6,1) RC( 6,2) RC( 6,3) RC(6,4)
            RC(2,0) RC(2,1) RC(1,2) RC(1,3) RC(1,4) RC(1,5)  RC( 8,0) RC( 8,1) RC( 7,2) RC( 7,3) RC(7,4)
            RC(3,0) RC(3,1) RC(3,2) RC(2,3) RC(2,4) RC(2,5)  RC( 9,0) RC( 9,1) RC( 9,2) RC( 8,3) RC(8,4)
            RC(4,0) RC(4,1) RC(4,2) RC(4,3) RC(10,0) RC(10,1) RC(10,2) RC(10,3) RC(9,4)
        >;
    };
};
